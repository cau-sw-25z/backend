#개발 서버 배포할 때 쓰는 설정
#나중에 개발 서버 생기면 채울 것.

spring:
  datasource:
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: ${JWT_SECRET}